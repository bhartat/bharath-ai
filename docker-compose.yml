# docker-compose.yml (FINAL - Definitive Network Fix)
services:
  backend:
    build: ./backend
    container_name: bharath-ai-backend
    ports:
      - "8000:8000"
    volumes:
      - ./backend:/app
    env_file:
      - ./backend/.env
    # The custom network with the bridge driver is the key fix
    networks:
      - bharath_ai_net
    depends_on:
      db:
        condition: service_healthy

  db:
    image: postgres:15
    container_name: bharath-ai-db
    restart: always
    env_file:
      - ./backend/.env
    ports:
      - "5432:5432"
    volumes:
      - bharath-ai-data:/var/lib/postgresql/data
    networks:
      - bharath_ai_net
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U $$POSTGRES_USER -d $$POSTGRES_DB"]
      interval: 5s
      timeout: 5s
      retries: 5

volumes:
  bharath-ai-data:

networks:
  bharath_ai_net:
    driver: bridge # This explicitly tells Docker to use the more stable network driver